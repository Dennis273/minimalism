# CLAUDE.md

本文件为 Claude Code 提供项目开发指南。

## 项目概述

Mini 照片社区（Mini Photo Community）是一个为 Mini 汽车爱好者打造的照片分享平台。用户可以上传拍摄的 Mini 汽车照片，系统自动提取 EXIF 元数据（拍摄时间、GPS 位置），构建一个全球 Mini 汽车照片墙。

详细产品文档见 `PRODUCT.md`。

## 目录结构

```
PRODUCT.md             # 产品需求文档
ARCHITECTURE.md        # 技术架构文档
CLAUDE.md              # Claude Code 开发指南
```

> 注：项目处于早期阶段，代码目录结构待项目初始化后补充。

## 常用命令

> 待技术栈确定后补充。

## 工作原则

- **基于事实和逻辑**：所有决策和实现必须基于已验证的事实，不得基于猜测或假设
- **遇到不确定时提问**：对需求、技术细节有疑问时，必须向用户确认，不得自行假设
- **禁止未经验证的工作**：不在未证实的信息基础上开展工作
- **方案必须经过 review**：任何实现方案都必须先向用户汇报并获得确认后才能开始编码。这是死命令，无例外。
- **文档先行**：所有工作开始前先更新相关文档
- **查阅官方文档优先**：编写任何配置文件或集成第三方服务前，必须先查阅官方文档确认正确格式，不得凭记忆或错误提示猜测
- **证明正确性**：任何配置文件改动，必须先向用户展示官方文档来源，证明配置是正确的，然后再执行改动。禁止先改动后查文档。

## Git 工作流

### 提交规范

- 在指定的功能分支上开发（分支名格式：`claude/<feature>-<session_id>`）
- 提交信息使用英文，简洁描述改动内容
- 推送使用 `git push -u origin <branch-name>`

### 创建 Pull Request

完成开发后，使用 GitHub CLI 创建 PR：

```bash
gh pr create --title "PR 标题" --body "$(cat <<'EOF'
## Summary
- 改动点1
- 改动点2

## Test plan
- [ ] 测试项1
EOF
)"
```

### PR 内容规范

```markdown
## Summary
- 简要描述改动（2-5 个要点）

## Changes
- 详细的改动说明（如有必要）

## Test plan
- [ ] 测试检查项
```

## 技术决策规范

当存在多个可行方案时，必须进行多维度对比分析后再做决策。

### 对比维度

| 维度 | 说明 |
|------|------|
| 代码复杂度 | 改动量、理解难度 |
| 可维护性 | 逻辑是否集中、是否易于修改 |
| 性能影响 | 运行时开销、渲染次数 |
| 扩展性 | 是否易于支持未来需求 |
| 架构一致性 | 是否符合现有设计模式 |
| 实现成本 | 开发时间、测试复杂度 |

### 决策优先级

1. **正确性优先**：代码逻辑必须完备，覆盖所有边界情况。不能为了改动小而写出有问题的代码
2. **架构一致性**：优先保持现有抽象，避免引入特殊分支（如 `if (providerId === 'xxx')`）
3. **可扩展性**：考虑方案是否能复用于类似场景
4. **最小改动**（辅助因素）：在满足上述条件的前提下，优先选择改动较小的方案。但绝不能为了改动小而牺牲正确性或架构

### 输出格式

```markdown
## 方案对比

| 维度 | 方案 A | 方案 B |
|------|--------|--------|
| ... | ... | ... |

## 推荐方案

**推荐方案 X**

**原因**：
1. ...
2. ...
```
